本地docker启动pgvector数据库并创建默认数据库vectordb
默认超级用户名：postgres
密码：postgres
```shell
docker pull pgvector/pgvector:0.8.3-pg18-trixie
docker run -d \
  --name pgvector \
  -p 5432:5432 \
  -e POSTGRES_PASSWORD=postgres \
  -e POSTGRES_DB=vectordb \
 pgvector/pgvector:0.8.3-pg18-trixie
```